name: "test"
on:
  push:
    branches:
      - 'main'
jobs:
  sonar-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: sonar-scan
        run: ./scripts/test.sh
          
          
# export SONAR_PROJECT_BASE_DIR=${{ github.workspace }}
